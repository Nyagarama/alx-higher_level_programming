-- A script that lists all records of the table `second table`
-- Database source is `hbtn_0c_0`in my MySQL
-- Order of display; Score then name

SELECT score, name FROM second_table ORDER BY score DESC;
